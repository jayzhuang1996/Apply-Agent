# Task Plan: Refine Web UI Dashboard

- [x] Remove the "Form Preview" (screenshot) section from the UI.
- [x] Improve the formatting of the "Resume Suggestions" box (render structured CSS).
- [x] Add endpoints in `app.py` for `/edit_field` and `/replace_resume` to interact with the paused Playwright session.
- [x] Update `index.html` decision bar to include buttons for "Edit Fields", "Replace Resume".
- [x] Implement modals or prompts in the frontend to capture the field edits or new resume file.
- [x] Restart the server to apply changes.
